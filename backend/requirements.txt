fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
numpy>=1.26.0
# pandas removed due to build issues on some Windows/Python combinations; install via conda if needed
# scikit-learn removed to avoid platform wheel issues; using numpy-based regression instead
pytest>=7.4.2
httpx>=0.27.0
